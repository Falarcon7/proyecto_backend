version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: db-estudio
    restart: always
    environment:
      POSTGRES_PASSWORD: mi_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: